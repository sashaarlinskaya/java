// 1. Удаление элементов массива: Удалите два элемента из массива, начиная с
// указанного индекса, используя метод splice(). 

let numbers = [1, 2, 3, 4, 5, 6, 7];
numbers.splice(2, 3);

console.log('Измененный массив:', numbers);